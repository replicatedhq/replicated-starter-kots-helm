apiVersion: kots.io/v1beta1
kind: HelmChart
metadata:
  name: vaultwarden
spec:
  chart:
    name: vaultwarden
    chartVersion: 0.1.0

  values:
    apps:
      vaultwarden:
        replicas: '{{repl ConfigOption `web_replicas` }}'
